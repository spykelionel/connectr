import { ResponseTemplateInterceptor } from './response-template.interceptor';

describe('ResponseTemplateInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseTemplateInterceptor()).toBeDefined();
  });
});
